const namespaced = true

const state = {
  counter: 0
}

const mutations = {}

const actions = {}

const getters = {
  counter: state => state.counter
}

export default {
  namespaced,
  state,
  mutations,
  actions,
  getters
}
